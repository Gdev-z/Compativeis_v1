{
  "buildCommand": "bun run build && node scripts/generate-static.js",
  "outputDirectory": "dist/client",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
